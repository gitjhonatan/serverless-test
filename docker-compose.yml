version: '3'

services:
  redis-serverless:
    image: redis
    command: redis-server
    hostname: redis
    ports:
      - "6379:6379"
  mongo-serverless:
    image: mongo
    restart: always
    ports: 
      - 27017:27017